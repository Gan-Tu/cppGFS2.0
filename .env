# Default docker compose profile: `docker compose up --build` starts the
# GFS Playground (web UI + supervised cluster in one container).
# For the classic one-container-per-server topology instead, run:
#   COMPOSE_PROFILES=cluster docker compose up --build
# (The two modes share host ports and cannot run at the same time.)
COMPOSE_PROFILES=playground
